language: cpp
os:
  - linux
compiler:
  - gcc
branches:
  only:
  - master
env:
  global:
    - COMPILER=g++-7

script:
  - cd $TRAVIS_BUILD_DIR/ECSTools   
  - make 
  - ls 
  - ./ECSTools
  - cd $TRAVIS_BUILD_DIR/Test
  - make
  - ./ESCTest
